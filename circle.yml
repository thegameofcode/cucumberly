machine:
  node:
    version: 0.12.7
test:
  pre:
    - npm start:
        background: true
    - npm run backdoor:
        background: true
    - cp src/config_sample.json src/config.json
  override:
    - npm test
    - npm run integrationTests
    - npm run acceptance
